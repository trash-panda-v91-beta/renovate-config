---
extends: default
ignore:
  - .venv/
  - .worktrees/
rules:
  comments:
    min-spaces-from-content: 1
  line-length:
    max: 100
  truthy:
    allowed-values:
      - "true"
      - "false"
    check-keys: false
